{
    "buildCommand": "cd intelli-cater && npm install && npm run build",
    "outputDirectory": "intelli-cater/dist",
    "installCommand": "pip install -r requirements.txt && cd intelli-cater && npm install",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}